{
  "name": "tinytask-mcp",
  "version": "1.0.0",
  "description": "TinyTask is a minimal task management system for LLMs exposed as an MCP server",
  "type": "module",
  "main": "build/index.js",
  "bin": {
    "tinytask-mcp": "build/index.js"
  },
  "directories": {
    "doc": "docs"
  },
  "scripts": {
    "build": "tsc && npm run copy-assets",
    "copy-assets": "mkdir -p build/db && cp src/db/schema.sql build/db/",
    "dev": "tsx watch src/index.ts",
    "start": "node build/index.js",
    "start:stdio": "TINYTASK_MODE=stdio node build/index.js",
    "start:sse": "TINYTASK_MODE=sse node build/index.js",
    "start:both": "TINYTASK_MODE=both node build/index.js",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage",
    "lint": "eslint src/**/*.ts",
    "format": "prettier --write \"src/**/*.ts\"",
    "clean": "rm -rf build",
    "prepare": "npm run build"
  },
  "keywords": [
    "mcp",
    "model-context-protocol",
    "llm",
    "agent",
    "task-management"
  ],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@modelcontextprotocol/sdk": "^0.5.0",
    "better-sqlite3": "^9.0.0",
    "express": "^4.18.0",
    "uuid": "^9.0.1",
    "zod": "^3.22.0"
  },
  "devDependencies": {
    "@types/better-sqlite3": "^7.6.0",
    "@types/express": "^4.17.0",
    "@types/node": "^20.0.0",
    "@types/uuid": "^9.0.8",
    "@typescript-eslint/eslint-plugin": "^6.0.0",
    "@typescript-eslint/parser": "^6.0.0",
    "@vitest/coverage-v8": "^1.0.0",
    "eslint": "^8.0.0",
    "prettier": "^3.0.0",
    "tsx": "^4.0.0",
    "typescript": "^5.3.0",
    "vitest": "^1.0.0"
  }
}
